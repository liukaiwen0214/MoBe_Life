<!--
  核心职责：定义小程序 `finance` 页面或组件的结构模板。
  所属业务模块：小程序展示层 / 结构模板。
  重要依赖关系或外部约束：模板中的字段和事件名需要与对应 JS 文件中的 data、methods 和组件属性严格一致。
-->
<view class="page">
  <view class="panel">
    <view class="panel-tag">刻度</view>
    <view class="panel-title">{{currentInfo.title}}</view>
    <view class="panel-desc">{{currentInfo.desc}}</view>
  </view>
</view>

<mobe-tabbar current="scale" current-sub="{{subKey}}"></mobe-tabbar>
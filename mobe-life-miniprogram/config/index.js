/**
 * 文件级注释：
 * 核心职责：集中管理小程序的全局配置，如 API 基础路径、版本号、功能开关等。
 * 所属业务模块：小程序基础设施层 - 配置中心。
 * 重要依赖：无
 * 设计约束：
 * - 配置项按环境区分（开发、测试、生产）
 * - 敏感配置（如密钥）不应硬编码在此文件中
 * - 通过 ES6 模块导出，支持按需导入
 */

/**
 * 全局配置对象。
 *
 * 设计初衷：将分散在代码中的配置集中管理，便于：
 * - 环境切换（开发/测试/生产）
 * - 配置变更时无需修改多处代码
 * - 版本管理和配置追踪
 *
 * 在架构中的角色：配置中心，被 request.js 等模块引用，
 * 作为应用运行的基础参数来源。
 *
 * 环境管理策略：
 * 当前使用静态配置，通过修改 baseUrl 切换环境。
 * 未来可扩展为：
 * - 根据小程序版本类型自动判断（开发版/体验版/正式版）
 * - 从服务端动态拉取配置
 *
 * 配置项说明：
 * - baseUrl：后端 API 基础路径，决定请求发送到哪个服务器
 * - version：小程序版本号，用于日志追踪和兼容性处理
 * - features：功能开关，控制某些功能的启用/禁用
 */
const config = {
  /**
   * API 基础路径，所有请求都会拼接在此路径后。
   *
   * 环境配置建议：
   * - 开发环境：http://127.0.0.1:8080（本地后端服务）
   * - 测试环境：https://api-test.mobe.life
   * - 生产环境：https://api.mobe.life
   *
   * 为什么不用 https（开发环境）：
   * - 本地开发通常使用 HTTP，避免自签名证书配置
   * - 生产环境必须使用 HTTPS（微信小程序要求）
   *
   * 注意事项：
   * - 路径末尾不带 /，拼接时由调用方添加
   * - 微信小程序要求正式版必须使用 HTTPS 和备案域名
   */
  baseUrl: 'http://127.0.0.1:8080'

  /**
   * 小程序版本号，遵循语义化版本规范（SemVer）。
   * 格式：主版本.次版本.修订号（如 1.2.3）
   * 用途：
   * - 日志上报时附加版本信息，便于问题追踪
   * - 后端根据版本号做兼容性处理
   * - 提示用户更新版本
   */
  // version: '1.0.0',

  /**
   * 功能开关配置，用于灰度发布和 A/B 测试。
   * 示例：
   * - newFeature: false（新功能默认关闭）
   * - debugMode: true（调试模式）
   */
  // features: {
  //   newFeature: false,
  //   debugMode: process.env.NODE_ENV === 'development'
  // }
}

export default config

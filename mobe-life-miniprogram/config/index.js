/**
 * 核心职责：集中维护小程序运行时配置。
 * 所属业务模块：小程序基础设施 / 配置中心。
 * 重要依赖关系或外部约束：`baseUrl` 直接决定所有接口请求目标；正式版必须切换到微信允许的 HTTPS 域名。
 */
const config = {
  /**
   * 后端 API 根地址。
   * 当前保留本地开发地址，方便小程序与本机后端联调。
   */
  baseUrl: 'http://127.0.0.1:8080',
  // baseUrl: 'http://119.91.216.175:8080',
}

export default config
